const jwtServiceConfig = {
  signIn: '/oauth/token',
  signUp: '/oauth/token',
  accessToken: '/admin/v1/access_token',
  updateUser: '/oauth/token',
  infoUser: '/admin/v1/user',
};

export default jwtServiceConfig;
